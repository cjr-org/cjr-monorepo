apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: auth-service
  description: Internal identity and access service for platform surfaces.
  tags:
    - auth
    - identity
spec:
  type: service
  lifecycle: experimental
  owner: group:identity-platform
  system: developer-catalog
